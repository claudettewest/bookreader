{
  "name": "book-reader",
  "version": "1.0.0",
  "private": true,
  "description": "A calm, beautifully focused web book reader.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
